# 📖 คู่มือ Setup โปรเจกต์ VibeCheck ทีละขั้นตอน

---

## 🎯 ขั้นตอนที่ 1: Setup Neon Database (5 นาที)

### 1.1 สร้าง Neon Account และ Project

1. **เปิดเบราว์เซอร์** ไปที่ https://console.neon.tech/
2. **Sign Up** ด้วย GitHub/Google/Email (ฟรีไม่ต้องใส่บัตร)
3. กดปุ่ม **"Create a project"**
4. ตั้งชื่อโปรเจกต์: `vibecheck` หรือชื่อที่ชอบ
5. เลือก Region: `US East (Ohio)` หรือใกล้ที่สุด
6. กดปุ่ม **"Create project"**

### 1.2 Copy Database Connection String

1. หลังจากสร้าง Project แล้ว จะเห็น **Connection String**
2. คลิกที่ `Connection string` → เลือก **"Direct connection"**
3. คัดลอก URL ที่มีหน้าตาแบบนี้:
   ```
   postgresql://username:password@ep-xxx-xxx.us-east-2.aws.neon.tech/dbname?sslmode=require
   ```
4. **เก็บไว้ก่อน** จะเอามาใส่ในขั้นตอนที่ 3

### 1.3 สร้างตาราง Database

1. ใน Neon Console คลิกที่ **"SQL Editor"** (เมนูซ้ายมือ)
2. วางโค้ด SQL นี้ลงไป:

```sql
CREATE TABLE IF NOT EXISTS daily_vibes (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id TEXT NOT NULL,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  luck_score INT CHECK (luck_score >= 0 AND luck_score <= 100),
  fortune_text TEXT NOT NULL,
  colors JSONB NOT NULL,
  song TEXT,
  UNIQUE(user_id, DATE(created_at))
);

CREATE INDEX IF NOT EXISTS idx_user_date ON daily_vibes(user_id, created_at DESC);
```

3. กดปุ่ม **"Run"** (หรือกด Ctrl+Enter)
4. ถ้าเห็นข้อความ **"Success"** แสดงว่าสำเร็จแล้ว! ✅

---

## 🔐 ขั้นตอนที่ 2: Setup Clerk Authentication (5 นาที)

### 2.1 สร้าง Clerk Account และ Application

1. **เปิดเบราว์เซอร์** ไปที่ https://dashboard.clerk.com/
2. **Sign Up** ด้วย GitHub/Google/Email (ฟรีไม่ต้องใส่บัตร)
3. กดปุ่ม **"+ Create application"**
4. ตั้งชื่อ: `VibeCheck` หรือชื่อที่ชอบ
5. กดปุ่ม **"Create application"**

### 2.2 เปิดใช้งาน Google OAuth และ Email

1. ใน Clerk Dashboard ไปที่เมนู **"User & Authentication"** → **"Social connections"**
2. เปิดใช้งาน **"Google"** (คลิกสวิตช์ให้เป็นสีเขียว)
3. ไปที่เมนู **"Email, Phone, Username"**
4. เปิดใช้งาน **"Email address"** (ให้เป็น Required)
5. เลื่อนลงมาเปิดใช้งาน **"Email verification link"** (Magic Link)

### 2.3 Copy Publishable Key

1. ไปที่เมนู **"API Keys"** (เมนูซ้ายมือ)
2. หาส่วน **"Publishable key"**
3. คัดลอก Key ที่ขึ้นต้นด้วย `pk_test_...`
4. **เก็บไว้ก่อน** จะเอามาใส่ในขั้นตอนที่ 3

---

## ⚙️ ขั้นตอนที่ 3: ตั้งค่า Environment Variables

### 3.1 แก้ไขไฟล์ .env.local

1. **เปิดไฟล์** `D:\Vibe_coding\apps\lms\.env.local` ด้วย Text Editor (VS Code, Notepad++ ฯลฯ)
2. **แก้ไข** 2 บรรทัดนี้:

```bash
# Clerk Authentication
VITE_CLERK_PUBLISHABLE_KEY=pk_test_ใส่_key_จริงที่ได้จาก_Clerk_ตรงนี้

# Neon PostgreSQL Database
VITE_DATABASE_URL=postgresql://ใส่_connection_string_จริงที่ได้จาก_Neon_ตรงนี้
```

3. **ตัวอย่างหลังแก้ไขแล้ว:**

```bash
VITE_CLERK_PUBLISHABLE_KEY=pk_test_Y2xlcmsuZXhhbXBsZS5jb20k
VITE_DATABASE_URL=postgresql://alex:AbC123dEf@ep-cool-darkness-123456.us-east-2.aws.neon.tech/neondb?sslmode=require
```

4. **Save ไฟล์** (Ctrl+S)

---

## 🚀 ขั้นตอนที่ 4: รันโปรเจกต์ (แค่ 1 คำสั่ง!)

### 4.1 รัน Development Server

1. **เปิด Terminal/Command Prompt**
2. **ไปที่โฟลเดอร์โปรเจกต์:**

```bash
cd D:\Vibe_coding\apps\lms
```

3. **รันคำสั่ง:**

```bash
npm run dev
```

4. **รอสักครู่** จะเห็นข้อความประมาณนี้:

```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

5. **เปิดเบราว์เซอร์** ไปที่ http://localhost:5173/

---

## ✅ ขั้นตอนที่ 5: ทดสอบใช้งาน

### 5.1 Login ครั้งแรก

1. หน้าแรกจะเห็น **หน้า Login** (LoginPage component)
2. เลือกวิธี Login:
   - **Google:** คลิก "Continue with Google"
   - **Email (Magic Link):** ใส่อีเมลแล้วกด "Continue" → เช็คอีเมลแล้วคลิก Link

### 5.2 เล่น VibeCheck ครั้งแรก

1. หลังจาก Login แล้วจะเห็น **MoodInput** (Textarea)
2. **พิมพ์ความรู้สึก** เช่น "วันนี้รู้สึกดีมาก!"
3. กดปุ่ม **"Check Vibe"**
4. จะเห็น **Loading Animation** 2 วินาที (Spinning Orb)
5. แล้วจะแสดง **Result Card** พร้อม:
   - ✨ Luck Score (0-100%)
   - 🎨 สี 3 สี
   - 💬 คำทำนาย
   - 🎵 เพลงประจำวัน

### 5.3 ทดสอบฟีเจอร์อื่นๆ

✅ **ทดสอบ "บันทึกเป็นรูปภาพ":**
   - กดปุ่ม "บันทึกเป็นรูปภาพ" ใต้ Result Card
   - จะดาวน์โหลดไฟล์ PNG ลงเครื่อง

✅ **ทดสอบ "ประวัติ 7 วัน":**
   - กดปุ่ม "ประวัติ 7 วัน" มุมขวาบน
   - จะเห็น Modal แสดงประวัติ (ตอนนี้จะมีแค่ 1 รายการ)

✅ **ทดสอบ "ห้ามเล่นซ้ำในวันเดียวกัน":**
   - Refresh หน้าเว็บ (F5)
   - จะเห็น Result Card เดิมโดยอัตโนมัติ
   - **ไม่มี** MoodInput ให้กรอก (เพราะเล่นไปแล้ว)

---

## 🐛 แก้ปัญหาที่พบบ่อย

### ❌ Error: "Missing Clerk Publishable Key"
**สาเหตุ:** ไม่ได้ใส่ Clerk Key ใน `.env.local`
**วิธีแก้:**
1. เปิดไฟล์ `.env.local`
2. ตรวจสอบว่า `VITE_CLERK_PUBLISHABLE_KEY=pk_test_...` ถูกต้อง
3. Restart dev server (กด Ctrl+C แล้ว `npm run dev` ใหม่)

---

### ❌ Error: "Failed to fetch today vibe" หรือ Database Error
**สาเหตุ:** Connection String ผิด หรือยังไม่ได้สร้างตาราง
**วิธีแก้:**
1. ตรวจสอบ `VITE_DATABASE_URL` ใน `.env.local` ว่าถูกต้อง
2. ไปที่ Neon Console → SQL Editor
3. รันคำสั่ง SQL ใน `neon-setup.sql` อีกครั้ง
4. Restart dev server

---

### ❌ Login แล้วไม่มีอะไรเกิดขึ้น
**สาเหตุ:** Clerk Redirect URL ไม่ถูกต้อง
**วิธีแก้:**
1. ไปที่ Clerk Dashboard → **"Paths"**
2. ตั้งค่า Sign-in URL: `http://localhost:5173/`
3. ตั้งค่า Redirect URL: `http://localhost:5173/`

---

### ❌ Tailwind CSS ไม่ทำงาน / หน้าเว็บดูแปลกๆ
**สาเหตุ:** Tailwind ยังไม่ build
**วิธีแก้:**
1. กด Ctrl+C ใน Terminal
2. รันคำสั่ง `npm run dev` ใหม่
3. ถ้ายังไม่ได้ ให้ลบโฟลเดอร์ `node_modules` แล้ว `npm install` ใหม่

---

## 🎉 เสร็จแล้ว!

ถ้าทำตามทุกขั้นตอนแล้วไม่มีปัญหา คุณจะเห็นหน้าเว็บ **VibeCheck** ที่สวยงามพร้อมใช้งาน! 🎨✨

---

## 📚 ขั้นตอนถัดไป (Optional)

### 🌐 Deploy to Vercel (Production)

เมื่อต้องการ Deploy ไปใช้งานจริง:

1. **Push โค้ดขึ้น GitHub:**
```bash
git init
git add .
git commit -m "Initial commit: VibeCheck app"
git remote add origin https://github.com/your-username/vibecheck.git
git push -u origin main
```

2. **Deploy to Vercel:**
   - ไปที่ https://vercel.com/
   - Login ด้วย GitHub
   - กด "Import Project"
   - เลือก Repository `vibecheck`
   - เพิ่ม Environment Variables ใน Vercel:
     - `VITE_CLERK_PUBLISHABLE_KEY`
     - `VITE_DATABASE_URL`
   - กด "Deploy"

3. **Update Clerk Allowed Origins:**
   - ไปที่ Clerk Dashboard → **"Paths"**
   - เพิ่ม Production URL (เช่น `https://vibecheck.vercel.app`)

---

## 💡 Tips

- **เปลี่ยนเพลง/คำทำนาย:** แก้ไฟล์ `src/lib/constants.ts`
- **เปลี่ยนสี Theme:** แก้ไฟล์ `src/index.css` และ `tailwind.config.js`
- **เพิ่มฟีเจอร์:** อ่าน Code ใน `src/App.tsx` และ Components อื่นๆ

---

หากมีปัญหาหรือคำถาม สามารถถามได้เลยครับ! 🚀
